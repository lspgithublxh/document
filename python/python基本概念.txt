1.问题：
	>web网站开发：任何语言、web框架必须解决三个问题：
		>a.url-函数/资源的匹配(函数/方法级别)   
			>同时url上数据的获取-截取{}
		>b.数据库的访问(获取数据，序列化) 
		>c.后端数据填充到模板(html + velocity)(非jsp时)。以及多种数据的返回：直接json,txt,html,图片。。。动态静态资源

		>d.request,response,session三大实体必须提供。fiter,incepter也应当可以提供。

	>django: 
		>安装和新建工程：pip install django   ；django-admin startproject projectname
		>启动项目：进入项目：python manage.py runserver 0.0.0.0:8088
		>视图模板：
			>条件判断{% if condition %} {% elif %} {% else %} {% endif %}
			>循环{% for a in alist %}
			>值比较： ifequal
			>包含其他html: include 'nav.html'
		>数据库对象模型：
			>
 	>django网站特点：
		>url和视图改变不用重启服务器，自动可以生效。但是view处理器改变了需要重启。服务器自动监测代码并自动重新载入，无需手动启动。
	>pyspark:
		>启动spark集群，在spark目录下执行：./bin/run-example SparkPi
			>执行：./bin/spark-submit --master local[4] SimpleApp.py
			     ./bin/spark-submit examples/src/main/python/pi.py
				./bin/spark-submit examples/src/main/r/dataframe.R
		>常用actions:	count()
		>常用transforms:	map reduce flatmap filter
		>结果收集：collect()
		>

	>机器学习：
		>numpy是c写的，所以速度保证快。
		>scipy是c写的，所以速度快。
		>scikit-learn构建在numpy scipy之上，所以速度也快。实现了经典的机器学习算法，如贝叶斯、随机森林
			>安装：pip install sklearn   pip install scipy  pip install matplotlib
			>
		>nltk:自然语言必须的一个库。
		>神经网络库：tensorflow\keras
		>顺序：scikit-learn -->  keras --> tensorflow

		>机器学习中的大量准备工作：
			>测试、验证
			>
	>pandas:install pandas  openpyxl pandas-datareader
		>数据类型：series  DataFrame对齐
			>series:索引和值对	
		>数据定位：行(iloc())、列(筛选loc())、单元格、区域、直接查询(query()) 其他：groupby() count()
		>数据操作：map()
		

2.常见问题：
	>安装好的包在pycharm中导入：即导入用户的site packages lib:即在settings /project incepter/ + / install to user site packages lib 勾选即可。
		>或者不用，直接pip安装即可，但是需要看到External Libraries并且只有这个库。
	>


参考资料：
1.https://www.cnblogs.com/yangzhang-home/p/6056133.html（python spark操作）
2.http://scikit-learn.org/stable/auto_examples/plot_cv_predict.html#sphx-glr-auto-examples-plot-cv-predict-py(官方例子)
3.https://blog.csdn.net/kevinelstri/article/details/62043338(机器学习，详细简易到难的资料和论述)
4.http://pandas-datareader.readthedocs.io/en/latest/remote_data.html#google-finance(官方资料，pandas)
5.https://blog.csdn.net/liufang0001/article/details/77856255(资料pandas)