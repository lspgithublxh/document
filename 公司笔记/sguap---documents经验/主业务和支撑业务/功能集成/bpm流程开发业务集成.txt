1.BPM也是一个网站系统。也要登录。且进去设计流程图，等。

  BPM系统提供了什么服务？
  1.1根据用户id，用户名，业务名，流程id创建一个流程，并返回这个流程id。同时也在这个BPM系统中启动了这个流程。
  1.2 根据用户id,用户名，业务名，工作项id。来标记完成任务项。（无返回值）。（相当于发送了一个流程，保存了这个流程）



2.UAP项目中使用它：
  引入一个jar包。使用相应的API。

3.前端直接向bpm系统发送创建流程信息：js中完成。使用：bpmintegrate.utils.BPMClient这个函数。
  事实上只