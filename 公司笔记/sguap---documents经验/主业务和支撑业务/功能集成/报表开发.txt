0.内容组成：
   
   在报表设计器里设计好报表，---应该会生成相应的数据文件（配置文件包括在内）。
   然后前端就可以看到设计好的报表了。
   其中报表设计器是在线工具。----在线设计报表结构（报表模板）（类似表格），其中数据行可以专门的配置（用数据源对象，其实就是一个sql结果集对象）
   //报表服务地址
  //报表也是一个html页面。提供一个url地址
 //可以嵌入页面中，整个报表可以配置（如对菜单项目）。使用uapreport.js可以实现。

1.提供什么服务？


2.开发过程：