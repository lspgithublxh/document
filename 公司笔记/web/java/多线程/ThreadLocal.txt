1.问题描述：
	>变量可以是属性，而存在固定区（对于多线程来说，这个区域的变量就是共享变量）；而不一定要存在方法栈里，当作局部变量。
	>一个方法帧里，局部变量是它自己的，每个方法帧里，有不同的局部变量（这些变量就不是共享变量）。
	>ThreadLocal类：是一个工具类，它的一个实例的功能，是将当前线程给它的数据保存在当前线程的threadlocals属性里（此属性是一个map,键是这个Threadlocal实例,值是要保存的数据）。
		>
	>用途：多线程调用静态方法中存储的变量是线程独立的，各自存各自的。
		>例如数据库回话Session的获取。