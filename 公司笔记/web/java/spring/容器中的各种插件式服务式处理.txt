1.位置（处理时机，向j2ee服务器注册时机，在该时机到了，则调用自己，自己开始处理）、规则、处理、输出。
	>比如国际化服务（filter）	。可以指明位置在哪儿，规则是什么，处理是什么，输出是什么。
	>比如安全服务（filter）

	>比如spring中注册的各种服务：
		>比如定时器：（创建bean就有初始化方法，从而开始初始化定时任务，启动了定时任务）
		>比如hibernate服务：....这个是手动调用---其他程序中业务程序中调用。还有事件激发调用。