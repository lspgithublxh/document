1.协议数据包：
 一般格式（比如11个字节的包）：
  flags:标记发送方的角色类型：比如是回复角色，还是请求角色。
  Commond Set:标记命令的种类，是什么类型的命令。
  Id:无状态的异步通信的协议，发送的命令包和对应回应的数据包需要一一对应，通过这个Id就可以对应，发送方就知道了，接收方也知道了。
  Length:标记一个数据包的长度，好隔断。
  Data:作为回复角色发送的包里的一个部分，就是请求的内容。
2.协议握手：（在连接建立之后）

3.协议状态：----看发送的数据包--命令数据包是否有顺序。


4.收发行为：是否同步


