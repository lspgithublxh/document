1.通信的过程：
  0) 回话建立：
  1) 每次对话：
     发送方：是一次一次的发送的。一次输入一些，点击发送。所以发送的消息必然有开始有结束，且上一次点发送而发送的消息绝不会和下一次点发送的消息耦合在一起，尽管可能达到的先后可能不一样。但一定是完整的一块一块的。一块对应着一次点击发送所发送的内容
		或者说一次flush的内容，和上一次的flush的内容在达到接收端的时候，inputstream有间隔--即在这个间隔内，inputstream里什么也没有。或者说一次网络接收放一次inputstream，下一次则再次放-----是否等inputstream为空之后才方进去??。
     接收方：是一次一次的接收的。一次接收发送方某一轮发送的全部。