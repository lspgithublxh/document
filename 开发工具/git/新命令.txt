1.问题：
	>http方式：
		>获取：git pull http://
		>上传：git push --set-upstream http://gitlab.58corp.com/lishaoping/house.united.hugwlistesb.git master
		>创建分支：git checkout -b newbranch
		>强行合并远程仓库里的一个分支：git pull http://gitlab.58corp.com/house.united/house.united.hugusercenter.git release:master
			尝试合并可以用git fetch ---

		>回退：git log
			git reset --hard  提交编码  如git reset --hard 0484928e3f8aa9a7fb744c387c3911f080eb8a90

		>远程分支fix_bug: 先pull让本地有远程分支的名称-红色。然后：
 		   >git checkout -b fix_order pro/stage
		>捡起其他分支上的某些提交到本分支上：git cherry-pick <C2_id>  即在本分支上直接cherry-pick其他分支的commit-id不用写分支名。git cherry-pick --continue持续提升。
		  >git cherry-pick a2b2b4be26145eec13d00135d3ae74e3f4f45203 3baf1e5f64b7fbb7ef1a8fbd36f21b1600ef16df
		  >含尾不含首： git cherry-pick 3baf1e5f64b7fbb7ef1a8fbd36f21b1600ef16df..da00b32c5098bf70cfd6132e6f5ab31f657c7b42
		    >撤销奇怪的问题：git cherry-pick --abort
		  >尤其注意：在看到某个file的修改history时，想要合并某些commit,注意要一个一个的合并，因为commit之间可能隔着其他的提交，这些提交没有修改本文件看不到，所有可能产生问题！！
		>丢弃分支上的某些提交：git rebase -i commit_c_id编辑drop或者#。
		 >git log查看提交。
		 >git rebase --abort 撤销
 		 >持续执行 git rebase --continue 来升高commit。
		------第二种方法：先git log 查看提交，后git rebase -i 打开某个提交之后，然后编辑把部分提交注释，然后保存退出add-commit有必要rebase --continue, 然后把原来的那个分支删除掉，把这个去掉该提交的分支提交上去。
		>没有push而撤销提交：可以reset
		>在git init/remote add/pull 之后，最好先将远程所有的分支名称先拉下来：git fetch rec 
		  >后面checkout -b 的时候就可以直接切换到那个分支了。
