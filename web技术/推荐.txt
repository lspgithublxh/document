1.推荐：
	>目的：从海量item中找出和用户(特征)匹配(需要、满意)的items。(y = F(Xi, Xu, Xc)) （用户特征、内容特征<关键词>、环境特征<地理，时间，item发布时间>）。提高推荐量和转化率。
	>算法1：items热度排序，推荐最热的给用户(默认他是大众用户)。(首次)(热度：点击率-浏览量、转发量、收藏量)
	>算法2：协同过滤算法。
	   >道理：即为什么可以达到目的：必要条件是什么、基本条件是什么、基本要求、基本约束是什么。
	   >判断：即怎样做可以达到目的：有道理的一种观点、符合事实的一种观点、大多数情况下都符合最遵从都满足都验证正确的一种观点。(得出方法：靠分析和猜想、假想、设想、预想、构想)
		>一个用户的同类用户喜欢的items也是该用户喜欢的items。(适合中间加入新加入的用户)
		 >用户相似度：同类用户-相似用户：用户本身特征相似、用户浏览相似、用户做法相似(评价、收藏)
		>一个用户喜欢的items的同类相似items也是该用户喜欢的：
		 >items相似度：相似度矩阵---余弦相似度
		 >相似度矩阵的改进：LFM:nm = nk * km 
		>一个用户喜欢的items所属于的更大类下的其他类的items也是该用户喜欢的：
		>拟合出一个“输入为产品多个特征，内部加权，输出为一个用户行为”的函数：user = f(pattr1,pattr2,pattr3,pattr4) 来得到一个用户对所有产品的喜好程度；这个喜好程度值是有说服力的。
		            (或者输入为用户的多个特征，内部加权，而输出对一种产品的用户行为)
		>用户标签：item标签，相匹配。则为用户与item的相似度。即用户和item统一映射到一个新的维度集合上去--在这些维度上用户有取值item也有取值。
	   >逻辑：即达到目的的行为过程：

	>ABTest: 开一个10%流量，5%为线上的，5%为新的算法策略；看用户的反馈，反馈为正则增加推广推荐，反馈为负则减少推广推荐。(看转化率：播放量/推荐量)













2.去处：
	>最适合我的：包容力最好的、结果导向的。
	>