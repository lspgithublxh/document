1.问题：
	>系统操作：
		>设置环境变量：set path = %path%;C:\python27
	>文件操作：
	>网络操作：
	>线程操作|进程操作：

	>任务杀死：taskkill /IM httpd.exe /F 
		    taskkill /pid 8504 -t -f  
	>任务查看：tasklist /fi "imagename eq nginx.exe"   比如杀死chrome.exe进程也是tasklist /F /fi "imagename eq chrome.exe"。
			tasklist|findstr "abc"
	>创建特殊文件：type nul>.gitignore

 	>端口查看：netstat -ano  或者：netstat -ano|findstr "16778"