1.k-means算法：
  》概述：一群点，求分类
  》必须认识的条件：
      >一个簇，有簇的中心：首次人为选择，后面计算出来。中心就是一个点。。
      >一个簇有若干个元素。元素就是输入的元素。
      >由一个簇中的元素，可以计算出簇的中心；
      >有若干个簇的若干个中心，可以对全部元素计算出它们新属于的中心。即属于的簇。
  -----某一轮和上一轮相比，簇的中心未变-或很小，或者轮数已经很高，那么分类完成。
  。。。算法过程略。


