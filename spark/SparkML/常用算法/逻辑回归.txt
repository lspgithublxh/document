类别：分类算法。有监督式学习。
1.解决的问题：
  >条件：有批注的数据若干。
  >预备：
	##1 回归：根据点，找拟合线。点回归到了线上。
	##2 线性回归：找拟合直线：y = ax + b;
	##3 逻辑回归：拟合出逻辑函数，使得输入x, 而输出要么是0，要么是1；或者输出是一个比例：0-1之间。是一个二分函数。
		      @@1 自变量的类型：一般为一个向量（比如平面上的任一点）（或者说类），而这个二分类函数，使得自变量的值域范围被分为两部分：这两部分只是它们各自对应的y值不同，与它们在值域上的分布情况无关---即其中一类值所构成的区域形状可以任意：比如如果这类是二维变量的点它们的区域可以是一个封闭的圆、三角形、正方形、椭圆、非封闭曲线的一侧（如二次曲线、一次曲线、指数函数、对数函数、三角函数、反三角函数）。。
			  
		      @@2 


	##4 可能性：事物、系统有事件，只要发生，就有可能性。
	##5 正态分布函数：
	##6 向量函数：最基本的如复数函数（复变函数）就是最基本的向量函数，拓展到多维变量也适用。
		      假定：向量之间也可以定义（有）加减乘除四则运算，而且计算方法至少有虚变量法（i,j,k）、平面上角度计算法（模和多个无方向角度,x1 / sqrt(x2 * x2 + x3 * x3)）、指数方法和变换方法
			    向量和常量之间也有计算方法（任然是向量）。
			    同轴上的数之间可以相互运算（四则）。
			    为什么产生了虚数？因为人工定义一个数i ,使得它有性质： i *  i = -1 ，即这是它和实数之间的关系，而所有实数是不满足这样的性质的，即这种数不是实数、不在实数轴上。而且 i * i = -1不能仅仅用数轴上的长度来代表它的值，或者它的全部，虽然说实数可以，但是显然，新的这种数，光用数轴上的长度是不能完全描述它的信息的、它的本质的，必须还使用其他信息（或许可以是角度,或者其他什么度）,暂先定义这种数是虚数,i是虚数，定义数值和虚数的相乘关系：5i也是一个虚数，5i - 4i = i。但是看到数值和虚数的相加：5 + 4i 是不能再化简的，即这种加号是和、与的关系，而不是数值之间可以加法运算的加号关系，但是它也表达了一个数，是某个方向上的数。
						既然i是一个数（虚数），那么它就有数轴，且发现 0 * i = 0,所以这个数轴和实数轴有一个交点:0。于是产生了一个疑问：虚数轴和实数轴之间的夹角、构成的角度是多少度？此外，实数轴和虚数轴确定的平面上除了两条轴以外的其他地方的如何标记？：发现可以用映射的办法一一包括进来，即平面上任意一个点可以用一个实数和一个虚数唯一确定，这种方式可以表示为：a +（和、与） bi，，这种表示是科学的，完整严密的，唯一的确定的确切的。
							开始思考虚数轴和实数轴之间的夹角，可以从它们之间的值上的联系启发： i * i = -1 。 类似实数轴，虚数轴必然也可以看作正负两部分，负号为了表示方向、用来表示方向，且根据下面的推论，实数可以统一表示为： a∠0°或者 b∠180°形式，这里统一以正实轴为底边，同理，虚数也应该可以用这种形式来表示，但是角度从上面可知虚轴和实轴相交了而已，只知道一定有角度（相交必然有相交角度）还不知具体角度，但这样的信息足够支持我们可以用长度-角度形式来表示虚数，为了统一表示角度，按照统一以正实数轴（实际上无论以哪条边为统一的底边都可以，没影响）为底边，从而表示出ai为：a∠x°,记为 ai = a∠x°（其中等号表达的是等效、相同、一致含义的意思，而不是说数值上要作一个比较，毕竟符合体系都不同，只是对应的实物描述的实物相同）,而内错角相等可知：-ai = a∠(x - 180)°。
							这样就统一了实数和虚数的表示形式（极其重要，必要，关键）,那么用该表示形式来表达 i * i = -1 这个含义，就是：∠x°* ∠x°= -1 = ∠180°，i * (-i) = 1, 就是 ∠x° * ∠(x - 180)°= ∠0°,也可以先看 1 * 1 = 1 ，和 2 * （-3） = -6 , -6 * 2i = -12i 的角度形式： ∠0°* ∠0°= ∠0°, 2∠0°* 3∠180°= 6∠180°,6∠180°* 2∠x°= 12∠180°∠x°= 12∠f(x,180)°,其实此时都是无法求解f的。。因为还需要关系：
							（常见结果发现角度之间相乘的结果是角度之和的关系，但未证明一般是否如此，下面来证）,为此，首先：实数轴和虚数轴之外的数（点）的表示形式，经上述，一可以表示为：a + bi （这里+是和、与的意思，整体映射对应到平面上的一个点，称该平面为复平面）,同理应该符合统一形式：x1∠x2°,经过直观的计算，可以知道两种形式之间的数值之间的转换关系（不谈），此关系：a + bi = sqrt(a * a + b * b)∠arctan(b/a)。定义该数为复数，而 i * i = -1 ，所以必然复数之间可以运算，而复数有角度表示方式，两边一替换，从而可以推知任意角度乘以任意角度之间的关系：∠x1°* ∠x2°=∠(x1 + x2)°,而根据以前认识，有：∠0°= 1。所以如果把角度看作是函数f,那么有f(x + y) = f(x) * f(y)，f(0) = 1,推知f(nx) = f(x)<n>,令x = 1, 则有f(n) = f(1)<n>，记f(1) = a, 有 f(n) = a<n>,可以证明n对实数成立，所以f(x)是指数函数，想向泰勒级数发展,或者拉普拉斯变换方向发展，不论。
							 那么，f(1) 是多少呢？（具体值上，因为a其实是一个复数）（是一个复数，就是 1 + arctan1i），这里其实是采用弧度制，而且从复数平面来看，它必然是一个复数。角度值是否可以是复数呢？即有没有虚数角度？（现实映照是什么）从虚数和虚数轴的引入类推，在复数平面扫的角是实数范围内扫的角，则在更高维度如立体空间中扫的角可能就是复平面的角了，为此，引入复数角的概念，或者空间角的概念。对于平面上的角，其实也可以认为角也有轴，但是这个轴是旋转的闭合的循环的圆，由于角的大小和角两边长度没有关系，所以这个圆轴任意，但是以0,0为圆心，轴的0点在x轴上：正实数轴上,角的大小等效于圆弧的长度。空间中一个点在三个平面上有三个投影点，从而有三个平面角，可以认为另外两个角是分量角，∠(a + bi + cj)°,如果仅仅用一个复数来表示角值：那么其实表达了空间中的一条直线（数值线）:∠(a + bi)°,但是如果还知道模长，那么就唯一确定空间中的那个点了，所以m∠(a + bi)°表示的是空间中的一个点（这个点已经不是复数了，是3维复数）。
							  所以，如果 f(n) = a<n>，中的n对于复数都成立（即有：f(z) = a<z>），那么这样的数，是三维复数（三维空间里面的数）里面的数。特别的，如果z特殊取值时，比如z = i, 那么相当于下面推论中说的空间旋转终边法，结果就是一个三维复数：即已经不是一个一般复数了。
							推论1：根据上述乘法规律，那么对于：i * (-i) = -1， 等价于：∠x°*  ∠(x - 180)°= ∠0°= ∠(2x - 180)°,得出 x = 90 ,从而说明虚数轴一定是和实数轴垂直的，这是由定义和统一表示形式决定的（否则得不到 i * (-i) = -1,尤其是 - i的定义和统一形式极其关键）
							推论2：解释∠(1 + i)°。两个复平面方式，和空间方式（两段方式，先x-y平面，定线，后往该线和z确定的平面运动，旋转两次，得到最终结果，确实是一个空间点），可以推至∠(a + bi)°。
								具体一个结论就是：∠πi = - 1，即a<iπ> = -1,且a此时应当表示为a = 1 + arctan1j,即虚数单位是不同的，一个复数角的，一个是复数的。
							推论3：根据欧拉公式，进一步有：e<iπ*有理数> 的值要么是+-1，要么是+-i。
							推论4：角度函数是指数函数，有指数规律，在复平面上看，一个角乘以另一个角等于两个角相加。具体化，一个角的底边终边长为1，角为θ,那么该点为cosθ + isinθ,它乘以它自己 = cos2θ + isin2θ。推广到n，则有 (cosθ + isinθ)<n> = cosnθ + isinnθ
							附加推论：p = αθ  （极径 = 极角的比例） ,即到原点的直线距离等于极角的若干比例倍的曲线---螺旋线。

			                        同理，一个事物在空间中的位置变化不只是长度变化，而且有角度变化。
						即是为了描述有向线段。而且本质上，任何实际线段都是有向线段，所以实数轴其实也可看做是有向线段：用长度值和角度值两个度量一起描述。以实数轴本身作为角度参照底边。那么一个正数线段可以看作是长度为正数方向为0度，表示为：m∠0°的有向线段，负数线段可以看做是长度为负数绝对值而方向为180度，表示为m∠180°的有向线段。
						推论1：上述应当有：m∠0°= - m∠180°（正数负数的关系），所以有∠0°= -∠180°。角度的值，角度的正负，角度的循环。角度的定义。从角度的定义和本质上看，角度的底边（其实边）是确定了的，而角度的不同是终边的不同，由于角度本身就是为了描述方向，（相对方向），所以相反方向的角度，角度值上应该只有正负的差异--或者非常简单的差异、类似数里面正负数的关系---借用数里面的负号的说明作用、表达的意思，用在这里，来表达相反角度之间的关系，表达式相反的这个意思（让其他人一看就明白）,而零度和180度的角度的终边是相反的，所以角度值上应该只有且是正负值的差异（甚至这是负号引进来的原因之一,除了多少的原因、比较的原因，比0还小的原因，0比它还多，客观也存在，且正是为了说明这种比0还小的情况）。
							这里的-号，是借用数里面的负号表达的数之间的相反关系，已经不是表示数值上的关系，而是表示两个角度之间的相对位置关系（方向之间的关系）--相反关系。
						推论2：不同方向上的数值就可以用角度来区分了，这样就可以描述不同方向上的数值了。∠θ°描述的是角度，不是值度。
						推论3：正实数和负实数只是两个方向上的数（角度值0和180）。而其他方向上的数事实上也存在，m∠θ°存在，而且这个角度值--θ值是一个范围在实数内的值。
						推论4：角度之间的关系：从借用相反关系的已有运算符号符号来表达角度之间的相反关系而确定角度之间的第一种数值关系：∠0°= -∠180°。和同终边重合关系而相等关系的：∠0°= -∠360°。
							又从角度的终边可以当作底边，即角度可以旋转而角度本身值不变而且描述也不变，且一个角的底边可以是新的一个角的底边，而新的一个角的终边可以是另一个角的终边，因为从样子上看完全符合角的定义，这就确定了新的角和旧的几个角之间的关系，角之间有关系，就在角度上量化体现了出来，从而有角度之间的叠加相等关系,但是这种视觉上的叠加关系，是改变了底边的，不是真正的相加，必须要用分量的表示方式才能正确表达这种视觉上的合成关系。
						推论5：有向线段的两种表示方式（二维）。
							@@1 实数
			    			推演：力是现实中的一种有向线段，可以通过力来研究有向线段之间的更一般的更深刻的更多的关系，或者角度之间的关系、角度之间的运算。
							力的相加减，平行四边形法则，其实和角度加减相似。							
						
			    >角度：角度是一种很重要的定义和发现，和长度一样重要。这两种度量极端重要。
				  角度和长度都是为了描述一个事物的空间位置信息、是空间唯一确定的最少描述、最少度量，是将事物放在空间当中，有了空间这个外在参照、相对于空间这个外在参照。
			          拿一个木棍来说，它的长度描述它区别于其他木棍在尺寸方面的情况是自身的东西--没有外界任何参照也有这个量度--是它的绝对信息，而它摆放的角度（比如相对于定义的一个x轴、线、另一根木棍）则描述的是它的相对信息，它相对于其他木棍的位置信息。
			    >虚数是什么？
			    >最大似然性：
					最大似然估计：模型定（分布模型），样本有，参数未定
					>阶乘的近似：n! ～ sqrt(2πn)(n/e)<n>
					>二项分布：A和B对战，A的胜率p,对战n轮，求A胜i次的概率。该公式即二项分布公式（离散）
						  根据前述：推知到p = 1/2时，i = n/2 和i = n/2 + d 时的近似概率。
						  再推知P(|X - n/2| <= csqrt(n) ) 这个中间区间的概率（的表达式），然后推知它的近似计算表达式。因为它是离散的连加和，将连加的变量和函数的变量统一，而看以看作后面跟个d(函数变量),因为d(函数变量) = 1。而当推广这个函数变量是连续的话（离散相当于是特殊的值）,可以将该概率公式变成积分函数，如果在把函数变量取值区间推向正负无穷，那么，概率公式变成完全积分公式：面积公式了，结果必然为1.
						  命令上述二项分布概率函数的连续化（极限化）形式为正态分布函数。
						  此外，当概率不是1/2，是p时，同样n到无穷，也有相类似的分布函数，而且记中心为Xp,那么Xn也会围绕Xp等参量而作正态分布：是为中心极限定理。
						  推论1：一个变量的分布概率，，即便它本身不是正态分布的，但是它参与为主的某个表达式整体作为变量却是正态分布的。该变量为随机变量。
							（随机变量的分布规律）
			   >数据处理方法：
					>取平均值法：
					>最小二乘法：
					 ##1 问题：一个因变量和其他n个自变量线性相关，且有多组这些自变量具体取值和对应因变量取值的样本，求线性系数（参数）。
						   @@1 根据观测有误差的思想，并构造累积误差公式：∑(观测值 - 理论值)<2> 。。（各个误差的平方和）
							并求误差最小时候的各个参数的值，此时当每个参数是变量，而必然要一个个的求，所以是求某个参量的偏导数为0时候的该参量的取值（因为是二次函数，只有一个最值，故可次）就是使得累积误差最小的该参量的取值。每个参量都这样求当它为变量时累积误差取最小值时它的取值，得到q个方程，每个方程里都有剩下的全部的参量，解这q个方程，刚好。
						       推论1：当只有一个变量时，使得累积误差最小，可以推知正好是取各个观察值的算术平均值（或许上述构造误差的平方和是根据这个来的，推广来的）
						       推论2：如果把观测变量当作某个变量的取值，那么这个变量是一个随机变量。那么这个随机变量的分布函数是什么？（线性关系的变量），如果目标变量的各个观测值也是某个变量的取值，那么这个变量是一个随机变量，那么这个随机变量的分布函数是什么？
					>随机误差的分布函数推导：
						>要明确的事情：什么是随机误差？---先准确的定义，找到它的本质。
						>随机误差有哪些性质？互相抵消、均值随样本增多而均值稳定、真值附近取值概率更大。
						>概率密度函数：就是该随机变量每个取值点的取值概率的度量函数。度量取值概率的函数：概率密度函数（分布函数）。
						>似然函数：每个样本误差的概率（概率密度来代替）的乘积，得到一个函数，这个函数是真值θ的函数，而样本是确定了的（概率函数是未知量,待求量.θ也未知，但当作自变量）,那么θ取不同值时，这个概率乘积的大小是不同的，可以很大也可以很小，但是显然很小时肯定不是真正的真值，很大时应该是真值，因为：样本必然围绕在真值周围，第二，样本必然均匀分布在真值周围（在等距离的线上应该是均匀分布的），这种均匀分布就会导致它们的误差的概率的乘积会取得最大值，而θ在其他地方的情况下的误差的概率都不会是最大值（和相等，每一项越平均，各项平均越大。这个真理。这里就是：概率和为1，每一项是一个样本的误差），，，，因为确信：真值越近取值概率越大。。。
							   所以使得似然函数取得最大值的θ值就是真值：至少是最接近的估计。此时有真值θ = h(x1,x2,...xn)（即是样本的函数）,,按照经验，这个函数h已经出来了，就是平均值函数。现在反过去看，什么样的函数可以满足这种：θ取真值时，似然估计函数将取极大值，真值θ = 样本平均值。
							   不难推出满足上述性质要求的函数：首先相乘，一定要让x1到xn有相加的机会，所以最少是指数函数，且θ在次数位置，第二，必须是一个单值高峰函数---最简单的是二次函数。光指数函数不行，而必须和二次函数结合起来，而二次函数作为次数位置的函数从性质是看，已经有最大值了：然后看它的极大似然估计：正好是样本的平均值。所以就是这样的函数。c * a< - m * x * x>	a > 1 （负号说明了这一点）,m > 0, c > 0 ，x代表误差e。。此外，全范围概率和应该为1。						   
							   ---推论1：求出随机误差的分布函数后，再看似然函数的样子，看到似然函数的最大值是误差的平方和取最小的时候————正是最小二乘法的关键等值性（关键等量关系）。
							   ---推论2：多个变量是不相关的随机变量，那么它们之和的分布也是正态分布，返回来成立，一个整体是正态分布，组成它的每个元素的分布也是正态分布（即概率密度函数是正态分布）。
							   ---推论3：所有同方差的分布中，正态分布的信息熵最大（即需要的知识(知识就是判定条件)最少，就可以构造出正态分布了）。
							   ---推论4：高尔顿挡板实验说明，社会中，生物学中，不同对象的同一度量的值构成的样本服从正态分布的原因是：遗传。（二项分布当遗传代数n趋于无穷代）....同理认为智商也会呈现二项分布（除非特殊因素的影响，如天灾人祸）。
		
				                >卷积：	（将会和图像处理相关）
						    >冲击函数：一瞬间的作用量，作用效果一定，时间可以很短更短，如冲量。
						    >信号分析：根据时域分析频域（计算频域）。
						    >信号处理：基于两点判断--事实：@@1 信号可以叠加 @@2 信号经过系统后会变化---本质即输入信号作为了影响因素因果关联而使系统的某一个度量的值受到影响而应激变化,而我们对这个度量进行实时数据采集，即作为输出信号。
								>系统的响应函数：
								>线性时不变系统：先要知道的原理：1)不同对象的同一种量在相互作用时，在任何作用位置（空间）的整体效果都是两个量分别效果的叠加，两个电子的电场、电势，两个机器对同一个物体的作用力，两个星球的引力场，这种整体效果是单独效果的相加的系统是线性系统。
												 2)时间函数的分解：傅里叶级数形式、无穷脉冲形式（注意每个脉冲的取值区间都是正负无穷，只是在非0处为0而已）。
												 3）t0时刻（当作0时刻）给系统输入一个信号x(t),持续到t1结束了，输出波形是y(t),t1时刻同时也把输出清空清0.然后输入同样一个信号x(t),输出为y(t)。如果采用同一个度量时间t,那么t1时刻输入的信号和t0时刻（作为0时刻）输入的信号x(t)之间的关系就是,t1时刻的信号表示为x(t - t1),输出为y(t - t1)。如果这样成立，那么说明系统在t1时间后没有参数变化,这是时不变系统。从取值上有这种关系。（正是为了计算，而统一时间度量）
												  推论1：单位脉冲响应，说明了输入结束后，系统仍然有输出，说明输入对以后有影响，t0时刻的输入甚至对t0以后所有时刻的输出都有一个影响 h(tx)（具体到某时刻，则是该时刻的h函数的值）。
												  推论2：由单位阶跃响应h[n]，而可以推测输入x[n],系统的输出y[n]。。通过计算y[5]就可以归纳出y[n]的计算表达式。y[n] = ∑x[k]h[n-k] （k作为变量,0<=k<=n）。。离散推广到连续，只需要认为离散时dx = 1， 而连续时dx = 任意小。y(t) = ∫x(k)h(t-k)dk （定积分，从 k = 0 到 k = t）。
								>傅里叶级数：用在信号分析中。把信号当作时间函数，而函数都可以作无穷傅里叶级数分解,，而对应的，一个时间函数也可以当作一个信号，一个信号可以当作无数个傅里叶级数时间函数形式的信号的叠加。为什么要这样看作，因为这个信号一旦输入一个线性时不变系统，那么它的效果就相当于该信号的每个傅里叶级数形式的时间函数描述的信号的单独作用的效果的叠加，而傅里叶级数形态的信号是非常简单的信号---频率单一明确。

								
								推论1：从度量上看，输入信号和输出信号其实已经不是同一个度量了，是分别的两个度量，两个相关的度量。。但都视为信号，只是作为系统的输入或者输出。
								推论2：可以积累能对信号作各种基本变换的系统,来对目标信号做出希望的变换。实际中，目标信号多是电信号，且一般有时域形式：函数形式。
						
						    
			   >统计学：研究的是对不同对象的同一度量的测量数据,对它们统计（统计数据）。而概率论、误差分析研究的对同一个对象的同一度量的多次测量数据（观测数据）。
				    （开始的时候统计学和概率论无关）					   


http://www.mamicode.com/info-detail-501714.html			

http://www.tuicool.com/articles/auQFju

http://blog.csdn.net/pakko/article/details/37878837

http://songshuhui.net/archives/76501（最大似然估计，自然推导）
http://songshuhui.net/archives/77386

https://www.zhihu.com/question/29461110（单位阶跃响应、单位脉冲响应）