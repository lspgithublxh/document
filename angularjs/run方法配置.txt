1.run方法有什么用处？   
1.1	设置全局对象，各个模块就可以共享了。
        尤其是多app的时候，每个app因为都有多个module,并用一个主module收集起来，而这个主module就可以用run方法来设置全局对象来给各个module使用。
       事实上就是各个模块被执行了，而主模块也被执行了。
       但执行过程包含几个动作：加载（加载依赖的模块）注册（依赖的模块）注入（依赖的模块）和执行回调函数（自己的回调函数，然后是依赖的模块的回调函数）。比如有先声明变量、对象、函数。
1.2侦听路由改变，执行回调函数：$rootScope.$on()
