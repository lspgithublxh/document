1.问题描述：
	>预知：
	>结构：日志型、key-value数据库
		>值的数据类型：支持存储字符串和三大集合数据类型操作---都是原子性的操作。value:最大512M
				-----值类型不同，操作命令稍微不同，因为操作动作是不同的---数据类型允许自己特定的操作
			  	>String:一般的，由图片转换而来的，序列化对象来的，等都可以。名：二进制安全
					一般的字符串操作函数--即命令也存在
				>List:以命令的方式就有list操作命令几种：但简单。
				
		>数据备份方式：
		>数据持久化方法：
		>数据同步到数据文件的数据更新频率：
		>发布订阅：
			>订阅频道：
			>发送消息到频道：
			>推送消息到订阅者：
		>原子操作：事务建立


	>性能参数：
		>读：11万次/s,写：8.1万次/s
		>发布订阅:


	>重要的几个为什么的问题、不那么必然的问题：
		>为什么高性能：
		>为什么把数据放在内存中：
		>所有操作为什么要设计为原子型的？怎么保证的：
		>为什么要存在内存中：可以进行复杂的操作---而代码简便。。而持久化到磁盘是追加的方式。

	>重要的几个一般问题、疑问：
		>困难在哪里？复杂地方在哪里？

	>重要的几个技巧、设计(带来、导致可靠稳定易扩展的工程特点)：
		>
	

2.小知识：
	>通信（服务器等待连接）、锁（锁池中等待）、IO（事件，用户输入等待）可以阻塞：所以可以利用这个来做发布订阅系统。


1.参考资料：
1.http://www.cnblogs.com/M-LittleBird/p/5902850.html（windows下的安装和使用）