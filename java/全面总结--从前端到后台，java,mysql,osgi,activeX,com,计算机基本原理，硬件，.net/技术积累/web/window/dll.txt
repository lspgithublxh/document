1.共享的可执行函数、数据文件。已经编译、链接。可以供多个进程在内存中使用--共享它的一个副本（而不用每个进程都加载一遍若干函数、数据，，节省空间）。
  且不可以单独执行。（因为没有主函数）。
  >包含有导出函数和内部函数（只供dll使用，外部不能调用）。
  >动态加载dll到内存中，返回导出函数的地址。
  .exe则有主函数，也可以在执行中加载dll文件中的函数到内存中
  在编译后的链接阶段，用动态链接而不是静态链接。

  .ocx仍然是动态链接库，是特殊的dll（可视化的控件，且定义控件可以引发事件）。