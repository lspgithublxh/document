1.某一列的类型是DropDownEditor，且它的name在字典中，且该列从后台传过来的值是逗号分割的，且这些值和前面的那个字典的value是有包含于的，那么显示时，就会显示字典的text值（同样逗号分割）。
  (表格是Grid类型，如果是其他类型比如Form，那么就会显示为下拉框格式了（和searchBox类似）)
 //是Editor，那么就可以加onchanged事件回调函数，且初始化的时候就会执行（对于Form就可以初始化选项了。）。


