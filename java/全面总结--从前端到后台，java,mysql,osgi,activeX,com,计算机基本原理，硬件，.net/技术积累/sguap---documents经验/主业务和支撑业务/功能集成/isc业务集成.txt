1.isc其实是一个网站。
  功能之一：用户管理（用户表，存储了用户。可增加修改。另外，可以合并账户）..

2.后端如果使用权限集成。引入一个权限包。（影响菜单，菜单包括：ToolBar，左边的子模块列表）
   该包的作用：根据用户id + 模块id，加其他的一些参数返回一个List<Function>。
   每个Function包括（name, text）两个属性的对象。到前端，则可以构造ToolBar里面的item（也可以构造其他菜单项。因为菜单项都最只需要这两个属性（但是text内容具体是什么？要配置，绑定什么功能？前端自己专门写））。所以，影响到的其实是ToolBar这个菜单而已。
   
  ----前端自己写。后端也只有一步是权限包里的内容。

