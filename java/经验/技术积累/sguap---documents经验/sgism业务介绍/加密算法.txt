1.MD5没有密钥，只能加密。

2.DES对称加密，用同一个密钥加密和解密

3.BASE64没有密钥，可以加密可以解密。

4.AES也是对称加密，用同一个密钥加密和解密。

5.RSA非对称加密，有公钥运算数和私钥运算数，和一个参与公钥私钥形成的模数。

6.SHA1只能加密，没有密钥。

7.SHA256只能加密，没有密钥。

8.SM2有公钥私钥，可以加密解密。

9.SM3没有密钥，只能加密。

10.SM4对称加密，用同一个密钥加密和解密。

1.3DES对称加密，用同一个密钥加密和解密。