1.问题描述：
	>预知：本身只是一个固定流程、固件、骨架，没有肉，流程中每个节点上没有实际起作用的对象。需要调用者注入功能对象，各个关节上的作用对象，流程中各个环节上的功能对象---职责实现者。
		本身只是一个骨架、一个模板、一个抽象描述。
		但描述骨架本身非常重要。甚至比关节实现者更重要。搭一个骨架，固定一个流程，填充实现者，这个系统就可以工作了，run运行了启动了运转了工作了提供服务了。